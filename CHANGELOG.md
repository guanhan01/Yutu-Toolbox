# 更新日志

## v0.1.4

### 界面

- 全面重做界面，接入 Miuix（`libs.miuix.ui`）设计语言：`core:designsystem` 的
  按钮、卡片、列表项、输入框、对话框、气泡、开关等基础控件整体替换为 Miuix 风格，
  主题、配色与尺寸令牌随之重排。
- 优化整体布局与页面切换动画。

### 修复

- 修复若干已知问题。

### 新增

- 对话页顶栏在「新建对话」左侧增加终端入口，直接进入 Linux 终端页；跳转由宿主
  注入（`onOpenTerminal`），`feature:home` 不依赖具体路由。

### 改进

- 对话页「思考能力」由档位列表改为底部滑杆。档位本身是离散有序的
  （Off < Default < … < Max），滑杆取值即档位下标，两端正好落在首尾档，不会停在
  半个档位上；顶部标签实时显示当前档位，两端标注首尾档。拖动只在跨档时写配置，
  一次拖动最多写「档位数」次，不按帧刷。

- 对话页待发送的图片附件由文件名胶囊改为缩略图。此前图片和 txt、zip 一样只显示
  文件名，选错图必须点进图库才能确认。现在图片按 56dp 缩略图展示（`ContentScale.Crop`），
  移除按钮压在右上角，其余附件类型仍是原文字胶囊。解码在 IO 线程做并按目标边长
  （168px）降采样后读入，避免相册原图直接进堆。
  注：仅覆盖待发送区；已发送消息本来就不回显图片（落盘时 `imageUris` 被清空，
  只留路径进文本上下文），本次未改动该行为。
- 终端入口与终端页标题统一为「终端」：此前入口叫「终端」、页面标题叫「打开终端」，
  同一处功能两个名字。
- 二级页顶栏前置按钮由汉堡改为返回箭头。此前二级页挂的是汉堡，但汉堡会打开抽屉，
  而抽屉里只有一级入口，从二级页打开抽屉后点任何一项都会先清掉二级栈，
  产生跳级返回；且它与系统返回手势方向相反，同一位置出现两套退出手势。
  现在一级页（对话 / 应用工具 / AI 设置 / 设置 / 关于）保留汉堡，
  其余二级页统一为返回箭头，行为与系统返回一致。涉及 AppShell 顶层路由
  与 Apps / Capture / Decompile / Database / Web / MCP / NetworkHub 各自的内嵌顶栏。

### 修复

- **修复 21 GB 幻影占用（真实存在，不是显示问题）**：执行任何 Linux 命令时会把
  `/dev`、`/proc`、`/sys`、`/storage/emulated/0`（约 20 GB）与系统分区 bind 进
  rootfs，而挂载从不卸载，一直留在应用进程的 mount namespace 里。系统的「用户数据」
  统计会跟随这些挂载穿透到整个外部存储与 `/system`，实测同一目录在应用命名空间下
  为 36 GB、全局视角只有 653 MB；`pm clear` 也删不掉（挂载随进程消亡，但统计已污染）。
  现改为在 `unshare -m` 的私有 mount namespace 内执行，命令结束（含超时被杀）时
  命名空间销毁、挂载自动消失。实测触发环境检测后残留挂载由 8 处降为 0。
  注：此前误判为「`sizeOf` 算法导致的显示虚高」，该问题确实独立存在（已按设备号
  剪枝修掉），但不是 21 GB 的成因。
  已被污染的统计不会随 `pm clear` 消失（该数字是系统侧持久化记录，不在应用数据
  目录里），需卸载重装才会归零；实测卸载重装后由 21.36 GB 变为 143 KB。
- 修复 Linux 环境页缺少卸载入口：此前只有「重新安装」，无法清除已装环境。现补上
  「卸载」按钮（危险操作二次确认），删除 rootfs、下载缓存与续装状态；执行前先解除
  该环境下的全部 bind 挂载并复核，残留未清则中止，避免 `rm -rf` 穿透挂载误删外部存储。
  按钮行由 `Row` 改为 `FlowRow`：加入第四个按钮后总宽超过卡片可用宽度，`Row` 不换行
  会把「卸载」挤出卡片直接裁掉（控件仍在、能点，但完全看不见），`FlowRow` 放不下时
  自动换行。
- 修复 Node.js 已安装却仍拦住 Codex / Claude：`usr/local/bin` 下的 `npm`、`codex`
  等是软链接，应用进程 stat 它们会被 SELinux 拒绝（`avc: denied { read } ...
  tclass=lnk_file`），跟随链接的 `File.exists()` 因此返回 false，组件被误判成
  「未安装」。改为在软链接场景回退到 `lstat`，并以 chroot 内实测出的版本号为准。
- 修复下载完整性校验只查「不够长」：续传时若服务端多给了内容，文件会变成
  「已下载部分 + 整个包」，长度校验照样通过，直到解压才失败，界面只剩一句
  「退出码 1」。现改为严格比对 `Content-Range` 声明的总长，并校验起点一致。
- 修复 zip 归档下载后未做完整性检查：损坏的包会反复拿去解压。现在下载完与复用
  本地副本前都会先验 zip 中央目录，坏了立即丢弃重下。
- 修复归档下载按声明顺序取第一个可用源：上百 MB 的包一旦选中慢源要多等很久。
  改为并发实测吞吐后排序，已下载可观进度的地址优先沿用。
- 修复换源后旧源的 `.part` 断点成为永不清理的残留。
- 修复 linux 工具安装跑在主线程上：Compose 的 `rememberCoroutineScope` 默认在
  Main，Java 侧 HTTP 下载会抛 `NetworkOnMainThreadException`，而该异常不带
  message，界面只显示「下载失败：null」。现统一切到 IO 线程。
- Codex / Claude 缺少 Node 时提前报错，不再白等一轮 `apt-get update`。

## v0.1.3

### 新增

- **AI 对话**：首页升级为完整的 AI Agent 聊天界面
  - 支持 13 家模型服务商，每个服务商可独立配置密钥与模型
  - 流式输出、深度思考（推理）折叠展示、跨服务商模型切换
  - 通过 function calling 接入内置 MCP 工具，思考过程实时显示工具调用卡片
  - 支持图片多模态输入，以及文件 / 文件夹 / 路径作为附件上下文
  - 会话历史本地保存，侧边栏可展开历史对话

- **消息操作**
  - AI 回答下方提供复制 / 重说 / 删除
  - 用户消息长按弹出复制 / 编辑 / 删除，编辑回到输入栏进行，可经加号追加图片与文件
  - 侧边栏历史对话长按删除

- **Linux 工具环境**：Debian / Alpine 按需安装，检测页展示组件状态，支持文件浏览、共享文件夹与终端

### 改进

- 品牌图标与视觉细节打磨，输入栏改为两行布局
- 附件 / 模型 / 思考三个入口改为锚定式溢出菜单，弹出时不再收起输入法
- 工具调用改为步骤卡片，推理块与工具调用按真实顺序展示
- API Key 加密存储

### 修复

- 修复 Linux 环境检测页 ANR、安装脚本空转、chroot 被 PRoot 抢占、无 DNS 等问题
- 修复 MITM 证书签发失败（ASN.1 WRONG_TAG）
- 修复模型 / 思考菜单弹出导致输入法收起
- 修复工具调用卡片在思考过程中被遮挡

---

## v0.1.1

- 关于页支持检查更新与开源项目入口
- 多项界面与稳定性修复

## v0.1.0

- 首个正式版本：文件、应用、网页、网络、数据库、反编译、MCP 客户端与内置 Server
